# Production dependencies (installed in the Docker image at runtime)

# CLI / output
rich>=13.0,<14.0

# Twitter
twikit>=2.1.0,<3.0
tweepy>=4.14,<5.0

# AI providers — pick what you need
openai>=1.40,<2.0
anthropic>=0.30,<1.0

# Scheduling & config
APScheduler>=3.10,<4.0
python-dotenv>=1.0,<2.0

# Utilities
pydantic>=2.6,<3.0
pyotp>=2.9,<3.0
