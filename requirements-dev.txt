# Development dependencies (NOT installed in the production Docker image)
# Includes production deps via -r, so `pip install -r requirements-dev.txt`
# gives you a fully working dev environment.

-r requirements.txt

# Testing
pytest>=8.0,<9.0
pytest-cov>=5.0,<6.0
pytest-mock>=3.12,<4.0

# Linting, formatting & type checking
ruff>=0.4,<1.0
mypy>=1.10,<2.0
