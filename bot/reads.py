"""Tweet search via twikit (scraping, cookie auth).

TODO (Phase 2):
  - login(username, password, totp) -> cookies.json
  - search_tweets(keyword, max_results) -> list[tweet dict]
  - filter: skip already-replied, skip own tweets, skip retweets
"""
